document.addEventListener('DOMContentLoaded',async()=>{const el=document.getElementById('lansen-content');if(!el)return;const d=await loadJSON('../data/lansen.json');el.innerHTML=`<div class="status-grid">${d.map(x=>`<section class="status-section"><h2>${x.title}</h2>${x.items.map(i=>`<div class="status-row"><b>${i.label}</b><span>${i.value}</span></div>`).join('')}</section>`).join('')}</div>`});
