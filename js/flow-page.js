document.addEventListener('DOMContentLoaded',async()=>{const el=document.getElementById('flow-content');if(!el)return;const type=el.dataset.type;const data=await loadJSON(`../data/${type}.json`);el.innerHTML=data.map(x=>`<article class="flow-item"><div class="flow-kicker">${x.id||''}</div><h3>${x.title||x.name||''}</h3><p>${x.description||''}</p>${x.logic?`<div class="logic-flow">${x.logic.map(s=>`<span>${s}</span>`).join('<b>↓</b>')}</div>`:''}<div class="subitems">${(x.items||[]).map(i=>`<div><b>${i.name||''}</b><p>${i.description||''}</p></div>`).join('')}</div></article>`).join('')});
