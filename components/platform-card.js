function createPlatformCard(p){return `<article class="platform-card"><div class="platform-head"><h3>${p.name}</h3><span class="tag">${p.lansen_status||'资料待补充'}</span></div><p>${p.description||''}</p><div class="field"><b>客户出现：</b>${(p.customer_appearance||[]).join('、')}</div><div class="field"><b>搜索：</b>${(p.search||[]).join('、')}</div><div class="field"><b>内容：</b>${(p.content_formats||[]).join('、')}</div><div class="field"><b>广告：</b>${(p.ad_formats||[]).join('、')}</div>${p.lansen_account?`<a class="text-link" target="_blank" rel="noopener" href="${p.lansen_account}">↗ 查看 LANSEN 当前账号</a>`:''}</article>`}
