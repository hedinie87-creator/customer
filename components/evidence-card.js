function createEvidenceCard(e){const link=(u,t)=>u?`<a class="btn" href="${u}" target="_blank" rel="noopener">${t}</a>`:'';return `<article class="evidence-card"><div class="evidence-meta"><span>${e.source_type||'待分类'}</span><span>${e.platform||'—'}</span><span>${e.source_date||'日期待补充'}</span></div><h3>${e.title||''}</h3><p>${e.description||''}</p><div class="evidence-image">${e.screenshot?`<img src="${e.screenshot}" alt="${e.title||'Evidence'}">`:'截图待补充'}</div><div class="evidence-grid"><div><b>客户看到：</b>${e.customer_sees||'待补充'}</div><div><b>客户可以：</b>${e.customer_action||'待补充'}</div><div><b>最终去向：</b>${e.destination||'待补充'}</div><div><b>状态：</b>${e.access_status||'待补充'}</div></div><p class="note"><b>事实说明：</b>${e.notes||'待补充'}</p><p class="note"><b>LANSEN关联：</b>${e.lansen_relevance||'待补充'}</p><div class="evidence-actions">${link(e.source_url,'↗ 原始页面')}${link(e.official_url,'↗ 官方说明')}</div></article>}
